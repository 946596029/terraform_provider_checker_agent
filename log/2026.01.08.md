# 2026.01.08 开发日志

## 阶段四
 * `目标` - 对接 MCP
  - 1. 使用协议 JSON RPC2.0, 对接 MCP
  - 2. 使用 docker 部署其他 mcp-server
 * `步骤`
  - 1. 引入 MCP 依赖
  - 2. 将 terraform 库克隆到本地
  - 3. 下载 `文件操作 MCP Server` 到 `WSL`
  - 4. 启动 MCP Server
  - 5. 对接 MCP Server 
  - 6. 利用 prompt 让 llm 搜索某一类文件
 * `完成情况`
  - 
 * `问题`
  - `mcp` 已注册的接口是一次性注册到 llm 里吗？那要如何解决上下文呢？
    - 会被一次性注入到 llm 的上下文中
  - `mcp` server 中不同的功能是怎样声明的？